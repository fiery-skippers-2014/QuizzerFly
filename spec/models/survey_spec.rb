require 'spec_helper'

describe Survey do

  # let(:user) { FactoryGirl.create(:user) }
  # before { @survey = user.surveys.build(desciption: "Lorem ipsum") }

  # subject { @survey }

  #   it { should respond_to(:title) }
  #   it { should respond_to(:description) }
  #   it { should respond_to(:user_id) }
  #   # it { should respond_to(:user) }
  #   its(:user) { should eq user }

  # it { should be_valid }

  # describe "when user_id is not present" do
  #   before { @survey.user_id = nil }
  #   it { should_not be_valid }
  # end
  # describe "with blank desciption" do
  #   before { @survey.desciption = " " }
  #   it { should_not be_valid }
  # end
  # describe "with desciption that is too long" do
  #   before { @survey.description = "a" * 141 }
  #   it { should_not be_valid }
  # end
end